-- main.lua


-- WHO AM I - GUI MULTI-FEATURE SCRIPT (Scroll + Cahaya +/- + Gesture)

local player = game.Players.LocalPlayer
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

-- LOADING SCREEN
local loaderGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
loaderGui.Name = "LoaderNotice"
loaderGui.IgnoreGuiInset = true
loaderGui.ResetOnSpawn = false

local bg = Instance.new("Frame", loaderGui)
bg.Size = UDim2.new(0, 260, 0, 80)
bg.Position = UDim2.new(0.5, -130, 0.5, -40)
bg.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
bg.BorderSizePixel = 0
Instance.new("UICorner", bg).CornerRadius = UDim.new(0, 12)

local text = Instance.new("TextLabel", bg)
text.Size = UDim2.new(1, -20, 0, 50)
text.Position = UDim2.new(0, 10, 0, 10)
text.BackgroundTransparency = 1
text.Text = "Harap menunggu...\nLoader sedang dimuat"
text.TextColor3 = Color3.new(1, 1, 1)
text.Font = Enum.Font.GothamSemibold
text.TextScaled = true
text.TextWrapped = true

local progressBarBg = Instance.new("Frame", bg)
progressBarBg.Size = UDim2.new(1, -20, 0, 6)
progressBarBg.Position = UDim2.new(0, 10, 1, -16)
progressBarBg.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
progressBarBg.BorderSizePixel = 0
Instance.new("UICorner", progressBarBg).CornerRadius = UDim.new(0, 3)

local progressBar = Instance.new("Frame", progressBarBg)
progressBar.Size = UDim2.new(0, 0, 1, 0)
progressBar.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
progressBar.BorderSizePixel = 0
Instance.new("UICorner", progressBar).CornerRadius = UDim.new(0, 3)

TweenService:Create(progressBar, TweenInfo.new(1.5, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
    Size = UDim2.new(1, 0, 1, 0)
}):Play()
task.wait(1.5)

loaderGui:Destroy()

-- MAIN GUI
local gui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
gui.Name = "FloatingMenu"
gui.ResetOnSpawn = false

local menuVisible = true

local mainFrame = Instance.new("Frame", gui)
mainFrame.Size = UDim2.new(0, 120, 0, 40)
mainFrame.Position = UDim2.new(0.5, -60, 1, -80)
mainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
mainFrame.BorderSizePixel = 0
Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 8)

local layout = Instance.new("UIListLayout", mainFrame)
layout.Padding = UDim.new(0, 4)
layout.FillDirection = Enum.FillDirection.Vertical
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
layout.VerticalAlignment = Enum.VerticalAlignment.Bottom

local function createButton(text, parent)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -10, 0, 30)
    btn.Text = text
    btn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.GothamBold
    btn.TextScaled = true
    btn.AutoButtonColor = true
    btn.Parent = parent
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    return btn
end

-- Tombol utama
local toggleButton = createButton("WHO AM I", mainFrame)
toggleButton.TextColor3 = Color3.new(1, 1, 1)

-- Frame tombol Senter dan Bypass
local kiriFrame = Instance.new("Frame", gui)
kiriFrame.Size = UDim2.new(0, 60, 0, 60)
kiriFrame.Position = UDim2.new(0, 20, 0.5, -30)
kiriFrame.AnchorPoint = Vector2.new(0, 0.5)
kiriFrame.BackgroundTransparency = 1
kiriFrame.Visible = false

local kananFrame = Instance.new("Frame", gui)
kananFrame.Size = UDim2.new(0, 60, 0, 60)
kananFrame.Position = UDim2.new(1, -80, 0.5, -30)
kananFrame.AnchorPoint = Vector2.new(0, 0.5)
kananFrame.BackgroundTransparency = 1
kananFrame.Visible = false

local function createIconButton(parent, iconId, tooltipText)
    local btn = Instance.new("ImageButton")
    btn.Size = UDim2.new(0, 60, 0, 60)
    btn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    btn.Image = iconId
    btn.Parent = parent
    btn.AutoButtonColor = true
    btn.ImageColor3 = Color3.new(1, 1, 1)

    local corner = Instance.new("UICorner", btn)
    corner.CornerRadius = UDim.new(1, 0)

    local stroke = Instance.new("UIStroke", btn)
    stroke.Thickness = 1.5
    stroke.Color = Color3.fromRGB(100, 100, 100)

    local tip = Instance.new("TextLabel", btn)
    tip.Text = tooltipText
    tip.Size = UDim2.new(1, 0, 0, 16)
    tip.Position = UDim2.new(0, 0, 1, 0)
    tip.TextColor3 = Color3.new(1,1,1)
    tip.TextScaled = true
    tip.BackgroundTransparency = 1
    tip.Font = Enum.Font.GothamSemibold
    return btn
end

-- Buat tombol
local senterBtn = createIconButton(kiriFrame, "rbxassetid://3926305904", "Senter")
local bypassBtn = createIconButton(kananFrame, "rbxassetid://3926307971", "Bypass")

-- WHO AM I toggler
local submenuOpen = false
local function toggleSubmenu()
    submenuOpen = not submenuOpen

    -- Warna teks merah jika aktif
    toggleButton.TextColor3 = submenuOpen and Color3.new(1, 0, 0) or Color3.new(1, 1, 1)

    -- Animasi muncul
    local tweenInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
    local visibleValue = submenuOpen

    for _, frame in pairs({kiriFrame, kananFrame}) do
        frame.Visible = true
        local targetSize = submenuOpen and UDim2.new(0, 60, 0, 60) or UDim2.new(0, 0, 0, 0)
        local tween = TweenService:Create(frame, tweenInfo, {Size = targetSize})
        tween:Play()
        tween.Completed:Connect(function()
            frame.Visible = submenuOpen
        end)
    end
end

toggleButton.MouseButton1Click:Connect(toggleSubmenu)

-- Gesture 3 jari
local fingerCount = 0
UserInputService.TouchStarted:Connect(function()
    fingerCount += 1
    if fingerCount >= 3 then
        menuVisible = not menuVisible
        mainFrame.Visible = menuVisible
    end
end)

UserInputService.TouchEnded:Connect(function()
    fingerCount = math.max(0, fingerCount - 1)
end)
